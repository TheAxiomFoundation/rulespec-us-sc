format: rulespec/v1
module:
  proof_validation:
    required: true
  source_verification:
    corpus_citation_path: us-sc/manual/dss/snap-policy-manual/page-324
  summary: 'The household must be given at least 10 days to submit verification. If
    late interview verification is received by the due date, allotment is prorated
    from the date of interview; if verification is received after the due date in
    Example #7, the household must re-apply with a new application.'
  deferred_outputs:
  - output: us-sc:policies/dss/snap-policy-manual/page-324#allotment_prorated_from_interview_date
    reason: Generated rule depends on an output that validation deferred because it
      flattened thresholded, capped, base-limited, or cross-referenced mechanics,
      or borrowed an unrelated same-named section term.
  - output: us-sc:policies/dss/snap-policy-manual/page-324#household_must_reapply_with_new_application
    reason: Generated rule depends on an output that validation deferred because it
      flattened thresholded, capped, base-limited, or cross-referenced mechanics,
      or borrowed an unrelated same-named section term.
  - output: us-sc:policies/dss/snap-policy-manual/page-324#verification_submission_period_satisfies_minimum
    reason: Generated rule used a local name that collides with a sibling RuleSpec
      export. This output is deferred until it can be encoded with a semantic branch-specific
      name without creating ambiguous sibling imports.
rules:
- name: minimum_verification_submission_days
  kind: parameter
  dtype: Count
  source: SNAP Manual page 324, late interview completed verification process
  metadata:
    proof:
      atoms:
      - path: versions[0].formula
        kind: amount
        source:
          corpus_citation_path: us-sc/manual/dss/snap-policy-manual/page-324
          excerpt: The household must be given at least 10 days to submit verification.
  versions:
  - effective_from: '0001-01-01'
    formula: '10'
